services:
  - type: worker
    name: insta-auto-sender
    env: python
    plan: starter
    buildCommand: "pip install -r requirements.txt"
    startCommand: "python app.py"

    disk:
      name: session-storage
      mountPath: /data
      sizeGB: 1
